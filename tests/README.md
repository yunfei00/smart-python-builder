# Test samples

- `tkinter_app.py`: Case A, standard-library GUI.
- `third_party_app.py`: Case B, isolated `requests` + `pandas` dependencies.
- `pyside6_app.py`: Case C, PySide6 GUI.

Use `run_phase1_tests.ps1` from the repository root on Windows.
